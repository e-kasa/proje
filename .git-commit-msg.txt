fix(stock-movement): hareket geçmişi tarih DESC sıralı (yeni → eski)

Sorun: Hareket geçmişi sayfasında kayıtlar rastgele sıralı
geliyordu — kullanıcı en yeni işlemi bulmak için listeyi tarıyordu.

Frontend (stock_movement_history_screen.dart):
- _filteredMovements getter sonunda createTime DESC sort eklendi.
- DateTime.tryParse + bDate.compareTo(aDate) ile yeni → eski.
- null tarihler sona alınır (defansif).

Backend (StockMovementControllerImpl.list):
- findAll() çağrısı sıralı dönmüyordu — stream sort eklendi.
- findByVariantId(...) ve findByVariantIdAndLocationId(...) zaten
  JPQL'de ORDER BY sm.createTime DESC kullanıyordu (StockMovement
  Repository), ek değişiklik gerekmedi.
- Backend defansif sort + frontend sort iki katmanlı garanti.

Sonuç: Kullanıcı sayfayı açınca son işlemi en üstte görür.

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
