import 'package:flutter/material.dart';

class AppColors {
  // === PRIMARY COLORS - Modern & Vibrant ===
  static const Color primary = Color(0xFF667eea); // Purple-Blue
  static const Color primaryDark = Color(0xFF5568d3);
  static const Color primaryLight = Color(0xFF8896f0);

  // === STATUS COLORS - Bright & Clear ===
  static const Color success = Color(0xFF10b981); // Emerald
  static const Color warning = Color(0xFFf59e0b); // Amber
  static const Color danger = Color(0xFFef4444); // Red
  static const Color info = Color(0xFF3b82f6); // Blue

  // === NEUTRAL COLORS ===
  static const Color dark = Color(0xFF1f2937);
  static const Color light = Color(0xFFf9fafb);
  static const Color white = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFe5e7eb);

  // === TEXT COLORS ===
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6b7280);
  static const Color textMuted = Color(0xFF9ca3af);

  // === BACKGROUND COLORS - Soft & Pleasant ===
  static const Color bgLight = Color(0xFFf9fafb);
  static const Color bgSuccess = Color(0xFFd1fae5); // Mint
  static const Color bgWarning = Color(0xFFfef3c7); // Light amber
  static const Color bgDanger = Color(0xFFfee2e2); // Light red
  static const Color bgInfo = Color(0xFFdbeafe); // Light blue

  // === ACCENT COLORS - Extra vibrant colors ===
  static const Color purple = Color(0xFF8b5cf6);
  static const Color pink = Color(0xFFec4899);
  static const Color indigo = Color(0xFF6366f1);
  static const Color teal = Color(0xFF14b8a6);
  static const Color orange = Color(0xFFf97316);
  static const Color cyan = Color(0xFF06b6d4);

  // === GRADIENT START/END COLORS ===
  static const Color gradientStart = Color(0xFF667eea);
  static const Color gradientEnd = Color(0xFF764ba2);
}
